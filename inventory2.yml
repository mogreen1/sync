---
all:
  
  children:
    servers:
      hosts:
        inventory2-with-yml-extension:
          ansible_host: 10.2.0.143
          ansible_user: ec2-user

      vars:
      ansible_user: ec2-user
      ansible_become: false
      ansible_become_method: sudo
